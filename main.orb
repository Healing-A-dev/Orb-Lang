@format "std.io";

set static master:Char = "K";
set static f:Any = "Hello";
set test:Array = {
    "Hello",
    "Moon"
};

static func Code.testFunction():{
    set Steam:Bool = false;
    if (Steam == true):{
        putln "How";
    }
}

if (f == "Hello"):{
    set static table:Array = {
        "String1",
        "String2",
        "String3"
    };
}

set ooga:Number = 900;
Code.testFunction();